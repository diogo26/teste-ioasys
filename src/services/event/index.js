const {create} = require('./create');
const {list} = require('./list');
const {get} = require('./get');
const {addFight} = require('./addFight')

module.exports = {
    create,
    list,
    get,
    addFight
}